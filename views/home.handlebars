<main id="home">
    {{>header}}

    {{#each data}}
    <article>
        <img src="../static/img/{{this.id}}.jpg" alt="Interieur van het restaurant">
        <div>
            <div>
                <h2>{{this.naam}}</h2>
                <p>{{this.prijs}}</p>
            </div>
            <ul>
                {{#each this.tags}}
                    <li>{{this}}</li>
                {{/each}}
            </ul>
            <p>{{this.omschrijving}}</p>
            <section>
                <form action="/dislike" method="POST">
                    {{!-- <input type="hidden" name="naam" value="{{this.naam}}"><br>
                    <input type="hidden" name="restaurantId" value={{this.id}}><br>
                    <input type="hidden" name="voorkeur" value="dislike"><br> --}}
                    <input type="image" src="../static/img/cancel.png" alt="Dislike button">
                </form>
                <form action="/like" method="POST">
                    {{!-- <input type="hidden" name="naam" value="{{this.naam}}"><br>
                    <input type="hidden" name="restaurantId" value={{this.id}}><br>
                    <input type="hidden" name="voorkeur" value="like"><br> --}}
                    <input type="image" src="../static/img/love.png" alt="Like button">
                </form>
            </section>
        </div>
    </article>
    {{/each}}
</main>